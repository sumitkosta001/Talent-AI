import { ResumeVisibility } from '../types/settings';

export const MOCK_RESUME_VISIBILITY: ResumeVisibility = {
  status: 'Public',
  hideContactInfo: false,
  allowDownload: true,
  allowSearch: true,
  allowContact: true,
};
